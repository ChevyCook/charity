[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
	name = Chevy  Cook
	email = chevrolet5624@gmail.com
[core]
	editor = code --wait
	autoclrf = True
